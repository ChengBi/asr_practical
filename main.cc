#include <cstdio>
#include "asr_ops.h"
#include "asr_array.h"
// #include "exception.h"
using namespace asr;

int main() {
	// asr::gaussian(1,1,1);
	int n = 10;
	Scalar<float> s(n);
	// printf("%d\n", s.size());
	return 1;
}
